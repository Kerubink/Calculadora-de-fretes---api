export const calcularFrete = async (cepOrigem: string, cepDestino: string, peso: string, comprimento: string, altura: string, largura: string) => {
  const valorFrete = 30.0; // Exemplo
  return { valorFrete };
};
